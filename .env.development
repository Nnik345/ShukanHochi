# Used only by `npm run dev` — requests go through Vite proxy (no browser CORS).
# Production / `npm run build` ignores this file; set full HTTPS URLs in Amplify.
VITE_API_FETCH_URL=/api/aws/default/fetchUserStats
VITE_API_UPDATE_URL=/api/aws/default/updateUser
